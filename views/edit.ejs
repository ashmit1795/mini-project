<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Update Post</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-start">
            <a class="text-blue-500 py-2 text-sm mb-5 inline-block" href="/app/users/profile">Go back to profile</a>
        </div>
        <h5 class="mb-5">Edit your post.</h5>
        <form action="/app/posts/edit/<%= post._id%>" method="post">
            <textarea name="updatedContent" class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%= post.content %></textarea>
            <input type="submit" value="Update Post" class="px-3 py-2 w-40 bg-yellow-500 text-black block rounded-md mt-2">
        </form>
    </div>
</body>
</html>